import { NumberonlyDirectiveDirective } from './numberonly-directive.directive';

describe('NumberonlyDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new NumberonlyDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
